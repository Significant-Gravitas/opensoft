diff --git a/flywheel/battleship/implementations/battleship_4.py b/flywheel/battleship/implementations/battleship_4.py
index abcdef1..1234567
--- a/flywheel/battleship/implementations/battleship_4.py
+++ b/flywheel/battleship/implementations/battleship_4.py
@@ -60,6 +60,10 @@ class Battleship4(AbstractBattleship):

     def create_turn(self, game_id: str, turn: Turn) -> TurnResponse:
+        if len(self.games[game_id].ships) != len(self.SHIP_LENGTHS):
+            raise ValueError("All ships must be placed before starting turns")
+
         row, column = turn.target["row"], turn.target["column"]
         hit_ship_type = self._is_ship_hit(game_id, row, column)
